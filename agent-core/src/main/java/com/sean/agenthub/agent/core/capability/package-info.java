/**
 * 模型供应商能力声明。
 *
 * <p>本包包含 ModelProviderCapability 枚举，用于声明 ModelProvider 适配器已覆盖的能力
 * （如 TEXT_CHAT、TEXT_STREAM、TOOL_CALL、STRUCTURED_OUTPUT 等），
 * 便于验收和诊断时明确当前 provider 的真实边界。</p>
 */
package com.sean.agenthub.agent.core.capability;
