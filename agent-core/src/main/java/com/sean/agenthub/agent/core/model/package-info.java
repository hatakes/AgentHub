/**
 * AgentHub 请求、响应、上下文和事件模型。
 *
 * <p>本包包含 Agent 对话链路中流转的所有数据对象：AgentRequest、AgentResponse、ModelRequest、
 * ModelResponse、ToolCall、ToolCallResult、AuditEvent、UserContext 等。
 * 这些模型不包含业务逻辑，只作为 Runtime、ModelProvider、Tool 和上层 API 之间的数据载体。</p>
 */
package com.sean.agenthub.agent.core.model;
