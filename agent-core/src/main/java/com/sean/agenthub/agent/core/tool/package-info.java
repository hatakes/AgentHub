/**
 * Tool Schema、Tool 上下文、Tool 结果和 ToolRegistry 实现。
 *
 * <p>本包包含 AgentTool 接口所需的支撑类型：ToolSchema（参数 Schema）、ToolSchemaProperty（字段定义）、
 * ToolContext（执行上下文）、ToolResult（执行结果）、ToolRiskLevel（风险等级），
 * 以及 ToolRegistry 的内存实现 InMemoryToolRegistry。</p>
 */
package com.sean.agenthub.agent.core.tool;
