我建议你直接把这个定义为：

AgentHub MVP - 知识库（RAG）模块 V1

目标不要做成 Dify。

目标是：

上传文档
↓
解析文档
↓
生成 Embedding（MiMo）
↓
存储 PostgreSQL + pgvector
↓
语义检索
↓
组装 Prompt
↓
MiMo 回答

⸻

项目目标

MVP目标

实现企业知识库基础能力：

文档上传
文档解析
文档切片
Embedding
向量存储
语义检索
RAG问答

暂不实现：

多模态解析
Agent Workflow
MCP Tool
权限体系
重排模型
知识图谱

⸻

技术栈

Java 21
Spring Boot 3.5.x
Spring AI 2.x
PostgreSQL 18
pgvector
MinIO
MiMo API

⸻

模块划分

agenthub-rag
├── document
├── embedding
├── vectorstore
├── retrieval
├── rag
└── common

⸻

一、文档管理

ai_document

CREATE TABLE ai_document (
id BIGSERIAL PRIMARY KEY,
kb_id BIGINT NOT NULL,
title VARCHAR(300),
source_type VARCHAR(30),
file_name VARCHAR(300),
file_size BIGINT,
file_md5 VARCHAR(64),
storage_type VARCHAR(30),
storage_object_key TEXT,
page_count INT,
parse_status VARCHAR(30),
create_time TIMESTAMP DEFAULT now(),
update_time TIMESTAMP DEFAULT now()
);

⸻

API

上传文档

POST /api/document/upload

支持：

pdf
docx
txt
md

返回：

{
"documentId": 1
}

⸻

查询文档

GET /api/document/page

⸻

二、文档解析

Parser SPI

public interface DocumentParser {
ParseResult parse(FileInfo fileInfo);
}

⸻

实现：

PdfDocumentParser
DocxDocumentParser
TxtDocumentParser
MarkdownDocumentParser

⸻

MVP：

PDFBox
Apache POI
CommonMark

⸻

输出统一结构：

public class ParseResult {
List<PageContent> pages;
}

⸻

三、Chunk切片

Chunk策略

固定窗口：

chunkSize=800
overlap=200

⸻

例如：

1~800
601~1400
1201~2000

⸻

输出：

Chunk {
Integer chunkIndex;
String content;
}

⸻

四、Embedding

Embedding SPI

public interface EmbeddingProvider {
List<Float> embed(String text);
}

⸻

MVP仅实现：

MimoEmbeddingProvider

⸻

配置：

agenthub:
embedding:
provider: mimo
model: mimo-embedding

⸻

五、向量存储

ai_document_chunk

CREATE TABLE ai_document_chunk (
id BIGSERIAL PRIMARY KEY,
document_id BIGINT NOT NULL,
chunk_index INT NOT NULL,
content TEXT NOT NULL,
token_count INT,
embedding vector(1536),
embedding_model VARCHAR(100),
create_time TIMESTAMP DEFAULT now()
);

⸻

pgvector索引

CREATE INDEX idx_chunk_embedding
ON ai_document_chunk
USING hnsw (embedding vector_cosine_ops);

⸻

六、知识库构建流程

上传文件
↓
MinIO
↓
DocumentRecord
↓
ParseJob
↓
DocumentParser
↓
Chunk
↓
Embedding
↓
ai_document_chunk
↓
READY

⸻

七、向量检索

Retrieval SPI

public interface RetrievalService {
List<ChunkResult> search(
Long kbId,
String question,
Integer topK);
}

⸻

流程：

question
↓
Embedding
↓
Vector Search
↓
TopK Chunk

⸻

SQL示例：

SELECT
content
FROM ai_document_chunk
ORDER BY embedding <=> :queryVector
LIMIT 5;

⸻

八、RAG问答

API

POST /api/rag/chat

请求：

{
"kbId": 1,
"question": "Spring AI 如何配置 DeepSeek?"
}

⸻

流程：

用户问题
↓
Embedding
↓
TopK Retrieval
↓
Context
↓
Prompt
↓
MiMo Chat
↓
Answer

⸻

Prompt模板：

你是企业知识库助手。
请仅根据以下资料回答。
资料：
{{context}}
问题：
{{question}}
如果资料中没有答案，请明确说明。

⸻

九、管理接口

重建Embedding

POST /api/document/rebuild

⸻

删除文档

POST /api/document/delete

逻辑：

删除 document
删除 chunk
删除向量

⸻

十、验收标准

上传：

100页PDF

能够：

解析成功
切片成功
Embedding成功
入库成功

⸻

问：

Spring AI 2.0 如何配置 DeepSeek？

能够：

命中相关Chunk
正确组装Prompt
调用MiMo
返回答案

⸻

这个版本控制在：

2~3周
5000~8000行代码

比较适合作为 AgentHub 第一个真正可用的 MVP。

后续路线：

V1
RAG
V2
Memory
V3
MCP Tool
V4
Workflow
V5
Agent
